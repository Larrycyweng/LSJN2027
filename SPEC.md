# 修改說明（prompt 形式）：LSJN2027 訓練紀錄 App，v0.1.0 → v0.4.2

以下文字可直接作為指令，交給任何開發者或模型，從 v0.1.0 改出 v0.4.2。每一條都是可驗證的要求。

---

## 0. 重新定位

原版（v0.1.0）把 App 當成「完整資料輸入介面」，追蹤器的每一欄都做成表單。改為：

> App 只做三件事：進度提醒、隨身知識庫複習、接收 Claude 產生的寫回包。所有文書輸入由 Claude 在對話中完成。使用者在 App 上必須親手輸入的，只保留「只有使用者能決定」的項目。

判斷標準：每一個要使用者操作的欄位，問「這是不是只有使用者能做？」不是的就移除，改由寫回包寫入。

## 1. 刪除

移除下列功能與對應程式碼，不保留隱藏入口：

- 題目紀錄表單（26 欄）與批次貼上
- 待檢討清單與盲審補填表單
- 錯誤指紋新增／編輯表單
- 診斷假設編輯表單
- 官方題接觸狀態的手動表單
- 上下文包產生器與複製按鈕（Claude 改為直接讀取 repo 的 raw JSON）
- xlsx 匯出與 SheetJS 依賴
- 學習紀錄表單中的欄位：LR 分鐘、RC 分鐘、檢討分鐘、重點能力、依計畫完成、專注、疲勞、前晚睡眠、備註
- 「設定」獨立分頁（併入「同步」）

## 2. 分頁結構

底部分頁改為五個，順序固定：`今日｜進度｜複習｜追蹤｜同步`。

## 3. 新增：今日

- 頂端顯示「今日任務」：起始週依 `settings.week_zero` 逐日顯示；之後依 `settings.week_plan` 的週次與星期角色顯示（週日驗證、週一學習、週二 LR、週三 RC、週四移轉、週五複測、週六休息）。
- 週次區塊顯示：階段名、週次、日期範圍、本階段每週預算；一列七格（日至六）顯示每天實際分鐘，今天有藍框，有紀錄的格子有底色。
- 表格保留：本週實際／預算、今日筆數、累計有效小時／372、待檢討題數、到期複習卡數、下一檢查點。
- 學習紀錄表單降為「手動補一筆（備援）」摺疊區，只留日期、有效分鐘、模式、來源四欄。
- 所有時數計算排除備註含「測試」的紀錄。

## 4. 新增：進度（checklist）

- 資料來源：`settings.checklist`，由計畫書產生。起始週逐日 7 項、第 1–17 週各 1 項，共 24 個可打勾項目。
- 四個檢查點（CP1–CP4）以醒目色塊內嵌在對應週次之後，顯示驗收標準與未達標處置，不可打勾。
- 依階段分組、可收合，當前階段預設展開，組標題顯示完成數。
- 頂端：完成數／總數、累計有效小時／372 與百分比、進度條。
- 每週項目顯示「本週實際／目標小時　累計至本週實際／累計目標」；累計目標為各週階段預算逐週相加。起始週每日項目顯示當日分鐘。
- 打勾狀態存 `data/progress.json`，可由寫回包 `mark_progress` 自動完成，也可手動。說明文字明示：打勾是排程提醒，不是能力證明。
- 「檢查點決定」區保留唯一使用者必填表單：選一個決定（繼續／調整／針對性修復／尚未驟證／已達就緒）＋證據摘要，保存時自動標記該檢查點完成。

## 5. 新增：複習

- 到期卡片：一次最多 5 張，先顯示標題，按「揭示」才顯示核心區分、常見誤判、修正動作、例子。
- 三個結果按鈕：記得（間隔 1→3→7→14→30 天）、不確定（隔天再出）、下次請 Claude 解釋（隔天再出並標記 `ask_chat=是`）。任何結果都不改變 `card_status`。
- 「快速筆記」摺疊區（備援）：標題＋內容兩欄，存為待確認卡片並標記 ask_chat。
- 知識卡片清單與錯誤指紋清單皆唯讀。

## 6. 追蹤（改唯讀）

- 頂端新增「弱點排序（官方題）」：依題型與能力標籤分組；每組樣本需 ≥ `settings.weakness_min_n`（預設 3）才排序；分數 = 錯題數 ×（1 − 正確率）+ 0.5 × 理由錯誤數；分數 > 0 者排序並標前三為 drill 候選；分數 = 0（無錯題）另列；樣本不足另列。原創題分開計算，不與官方題混合。
- 官方題累計統計、接觸狀態、診斷假設皆改為唯讀清單。

## 7. 同步

- 「寫回包」置於頁面最上方：貼入 JSON → 「檢查」逐筆列出可寫入／拒絕原因 → 「確認寫入並同步」。寫入後若已設定權杖則自動執行同步。
- 冪等：每筆需 `client_request_id`；已存在於任何資料檔者拒絕；包內重複者拒絕。
- 允許操作：`add_session`、`update_session`、`add_attempt`、`update_exposure`、`add_card`、`add_handoff`、`add_signature`、`suggest_signature_update`、`suggest_hypothesis_update`、`mark_progress`。
- 限制：`add_attempt` 為官方題時必須含 `source_evidence: "使用者截圖"`；`mark_progress` 不得指向檢查點；`add_card` 的狀態一律設為待確認；`suggest_*` 只能改政策檔列出的欄位；不提供刪除或覆寫作答。
- GitHub 同步、本機資料、版本資訊置於同頁下方。

## 8. 資料層

- 新增 `data/cards.json`、`data/handoffs.json`、`data/progress.json`、`data/write_policy.json`。
- `settings.json` 新增：`week_zero`、`week_plan`、`checklist`、`weakness_min_n`、`options.card_status`、`options.review_result`；`options.source` 改為 `LawHub／LSAT Lab／專案對話／其他`。
- 合併規則不變：以 id 為鍵，`updated_at` 較新者為準。

## 9. 離線元件

- `sw.js` 改為網路優先、斷網才用快取；快取名稱隨版本更新。原版「快取優先」會使介面更新後仍顯示舊版，不可再用。

## 10. 文件

- `README.md`：改寫「分工」與「每日流程」；每日流程限定為：看任務 → 做題 → 截圖給 Claude → 貼寫回包（兩次點擊）。
- `WRITEBACK.md`：記錄寫回包格式、每次課後至少包含的操作、`mark_progress` 的 item_id 規則。

## 11. 驗收條件

用手機尺寸（390×844）的瀏覽器驗證：

1. 五個分頁名稱與順序正確；無任何 v0.1.0 表單殘留。
2. 貼入含 10 種操作的寫回包，全部可寫入；重貼同一包，全部被拒絕。
3. 官方題 `add_attempt` 未附截圖證據被拒絕；`mark_progress` 指向 `cp1` 被拒絕。
4. 進度表 24 項可打勾，寫回包 `mark_progress` 後對應項目已勾。
5. 今日頁逐日格與進度頁每週「本週／累計」時數與資料一致；標為測試的紀錄不計入。
6. 卡片按「記得」後 `next_review` 延後且 `card_status` 不變。
7. 修改 `index.html` 後重新載入一次即顯示新版。
